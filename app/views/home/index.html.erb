<h1>Home baby</h1>
<p>This is the main  page for the crypto app</p>

<button type="button" class="btn btn-primary">Start</button>
<br/><br>
<% @coins %>
    <% for x in @coins %>
         <% for coin in @my_coins  %>
            <% if coin == x["symbol"]  %>
            <%= x["name"]%> :| $ <%= x["price_usd"]%> :| <%= x["market_cap_usd"] %> :|</br> <%= x["rank"]%></br></br>
        <% end %>
    <% end %>
<% end %>